# Root .env — used by docker-compose.
# Copy to .env and fill in values before running `docker compose up`.

# HMAC key for snapshot signing — use a long random string in production.
SNAPSHOT_SECRET=change-me-to-a-long-random-string

# Set to true to enable test/admin endpoints (/api/v1/test/*).
TEST_MODE=false
