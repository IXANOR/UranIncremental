# Local development (without Docker):
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/uran_incremental

# Docker: DATABASE_URL is set directly in docker-compose.yml (db service name).
# Use the root .env.example for Docker secrets instead of this file.

SNAPSHOT_SECRET=change-me-to-a-long-random-string
TEST_MODE=false
